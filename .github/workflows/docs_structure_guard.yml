# INV-DOC-*: Docs repo root structure guard.
# Root allowlist, archive path format, README current links, analysis report limit.
name: Docs structure guard

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  secret_scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  check-docs-root:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      # Pin Ruff to match .pre-commit-config.yaml so local pre-commit and CI behave the same.
      - name: Install ruff
        run: pip install "ruff>=0.8.4,<0.9"

      - name: Ruff check
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

      - name: Run docs root invariants
        run: python .github/scripts/check_docs_root.py

      - name: INV-CI-COMPLY-2 (CI compliance check)
        run: python tools/check_ci_compliance.py --workspace .
